# Codex Link 2.1 · Windows 交付

UI 基于用户选定的 UI-01。此次针对用户 2026-09-14 标注的四处问题做局部修复，保留原动态 CL Logo、折射背景、渐变色和圆润玻璃按钮。当前先交付 Windows；Mac 验证按用户安排后续进行。

## 修复内容

1. 创建备份：进度卡不再整卡吸顶；长文件名自然换行，标题随宽度收敛。最小桌面窗口与 402px 预览均检查了进行中状态。
2. 从备份恢复：类别外层和展开内容不再各自限定滚动高度；页面承载主滚动，长对话明细在所属项目内单独滚动。展开 252 条模拟对话时，后续类别仍按文档顺序显示。
3. 窗口尺寸：桌面最小尺寸改为 900×620；1050px 以下的流程区改为单栏，720px 以下的浏览器预览使用底部导航和完整内容宽度。
4. 备份管理：详情卡不再吸顶，滚动时不会覆盖事务回滚点或安全状态。

## 交付物

- 安装包：`dist/Codex-Link-Setup-2.1.0-x64.exe`
- SHA-256：`b6a8ed77198feaac173fd9fafb9d7252151e42ef1d951a8669817ed53d6b527b`
- 校验文件：`dist/Codex-Link-Setup-2.1.0-x64.exe.sha256.txt`
- 交付清单：`dist/UI01-2.1-delivery.json`

## 验证

- `npm run check` 通过；`npm test` 52/52 通过。
- 真实 Electron 界面交互 8 项通过，含创建备份和在隔离目标恢复一条对话；Logo 与减少动效 4 项通过。
- 402×874、900×620、1024×768、1340×860、1440×1000 的五页截图没有横向溢出或控制台错误；长列表和吸顶回归 4 项通过。证据位于 `ui-verification/ui01-2.1/`。
- 最终安装包在独立临时目录完成安装、启动、真实备份、完全退出重启、外观保留和卸载；备份在卸载后仍存在。安装包 SHA-256 与运行报告一致，打包界面和后端源码逐文件哈希一致。

以上是 Windows 本地交付验证。独立用户评审尚未提交，Mac DMG 和启动验证尚未进行；本说明不把它们记为通过。
