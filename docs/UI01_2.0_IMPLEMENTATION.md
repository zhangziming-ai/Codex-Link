# Codex Link 2.0 · UI-01 实现与交付

更新：2026-09-11。采用用户选择的 UI-01。用户已确认本次先交付 Windows，Mac 验证另行安排。

## 已实现

五页艺术排版、有色圆润玻璃按钮、原流动 CL Logo、局部折射背景、深浅/系统外观、减少动态效果。管理页加入恢复点搜索和选中详情。备份汇总与所选分类联动；恢复操作紧邻实际计划。外观保存在本机配置中，应用端口变化后仍然保留。

原有备份、逐条恢复、SHA-256 完整性校验、事务回滚、凭据永久排除及跨系统适配功能继续使用真实后端。

## 当前产物

- Windows：dist/Codex-Link-Setup-2.0.0-x64.exe。
- 解压运行目录：dist/win-unpacked/。
- Mac 后续安排；当前不交付或声称已验证新的 Mac 应用包。
- 设计验收：design-qa.md。
- 截图与交互证据：ui-verification/ui01-2.0/。
- 本地演示：运行 node scripts/ui01-preview-server.js 后访问 http://127.0.0.1:4391；仅使用独立测试资料。

## 验证结果

- npm run check：通过。
- npm test：52/52。
- UI 交互：8 项通过，含创建真实备份和单条对话恢复至隔离目标。
- 原 Logo / 减少动效：4 项通过。
- 四种窗口尺寸 × 五页：无横向溢出。
- Windows 打包应用：桌面接口、真实备份、完全退出重启和跨端口外观保留通过。
- 临时目录安装与卸载：安装成功，卸载后备份保留；详见 installer-smoke.json。
- 阶段自检：自动项 76/76；总判定 INCOMPLETE，不能当作正式发布通过。

## 仍须外部完成

VERSION_2.0_ACCEPTANCE.md 第 12 项要求 Apple Silicon 的最终 DMG/ZIP 和启动至少 8 秒证据；当前 Windows 主机无法完成。第 13 项要求独立首次使用者评审，尚未收到实际评审记录。旧 release 目录中的同版本 Mac 包不代表本次 UI-01 已验证。

后续在 Mac 使用当前项目源码运行 npm run package:mac:arm64；或使用 .github/workflows/build-macos-arm64.yml。不要把源码 ZIP 当作可直接运行的 Mac 应用。

## 复查命令

先启动隔离演示服务，再执行：

```powershell
.\node_modules\.bin\electron.cmd scripts/ui01-interaction-test.js
.\node_modules\.bin\electron.cmd scripts/ui01-motion-test.js
.\node_modules\.bin\electron.cmd scripts/ui01-capture.js
node scripts/ui01-package-smoke.js
```

这些命令需要可启动 Electron 和创建测试目录的本机环境。所有运行测试先验证输入属于专用 fixture。
